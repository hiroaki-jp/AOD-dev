# ADR 0001: TODO本文に加えて description を任意項目として持たせる

## 決定

TODO作成APIの入力とTODOレスポンスに description を含め、nullable な任意項目として扱う。

## 根拠

スコープ外に詳細編集機能はないが、初期登録時点で補足情報を保存できる方が実運用上の利便性が高い。必須にすると既存の最小要件を超えるため、任意項目として後方互換性を保つ。
