# ADR 0001: TODO の永続化方式はリレーショナル DB を採用する

## 決定

todos テーブルを持つ RDB を正として、一覧/追加/完了更新を単一テーブルで永続化する。

## 根拠

要件は単純な CRUD に近く、永続化と再起動後保持が必要である。単一利用者・低〜中規模利用では RDB が実装・運用・整合性のバランスが良い。編集/削除/共有など将来スコープ外の機能を含まないため、まずは最小構成の単一テーブルで十分である。
